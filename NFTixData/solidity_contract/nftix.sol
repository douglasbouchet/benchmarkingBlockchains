pragma solidity >=0.7.0;

contract NFTix {
    //int256 private count = 0;
    //
    //function push(int256 delta) public {
    //    count += delta;
    //}
    //
    //function pull(int256 delta) public {
    //    if (count > delta) {
    //        count -= delta;
    //    }
    //}
    function primarySale(int256 ax) public pure {
        uint32 counter = 0;
        counter -= 1;
    }
    // function primarySale(
    //     address _destinationAddress,
    //     address _eventAddress,
    //     uint256 _primaryPrice,
    //     uint256 _basePrice,
    //     uint256 _orderTime,
    //     bytes32[] memory _ticketMetadata
    // ) public pure {
    //     uint32 counter = 0;
    //     counter -= 1;
    // }
}
